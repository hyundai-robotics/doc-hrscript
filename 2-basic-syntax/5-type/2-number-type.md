# 2.5.2 数值数据类型

数值数据类型存储整数或实数。让我们使用打印语句进行打印。如果在打印语句中列出多个用逗号 \(,\) 分隔的值，如下例所示，则每个值将以空格分隔显示。

```python
280
3.141592
-99
print 280, -99
```

在系统内部，整数和实数分别处理。每种数据大小如下：

| 数据类型 | 数据大小 \(字节\) |
| :--- | :--- |
| 整数 | 4 |
| 实数 | 8 |