# _spd_rate 변수

재생속도 비율을 읽거나 설정합니다.

### 설명

조건설정 - 자동운전 속도비율와 동일한 설정이며 단위는 %입니다. 1~100 의 값을 설정할 수 있습니다.

### 문법

```python
var res
res = _spd_rate
```

### 사용 예

```python
   ...
   # 재생속도가 50% 미만이면 100%로 올린다.
   if _spd_rate<50
     _spd_rate=100
   ...
   end
```
