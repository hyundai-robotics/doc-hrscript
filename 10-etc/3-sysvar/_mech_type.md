# `_mech_type`

读取当前选择的机器人类型。

### Description

它不能作为赋值语句的左侧。

支持的版本为 V70.02-00

### Syntax

```python
var res
res = _mech_type
```

### Sample

```python
   ...
   # 打印当前的机器人类型。
   print _mech_type
   ...
   end
```