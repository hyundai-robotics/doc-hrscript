# `_acc_rate`

获取或设置速度曲线中的加速度。

### 描述

- 单位 : %
- 范围 : 1 到 100
- 默认值 : 100

### 语法

```python
var res
res = _acc_rate
```

### 示例

```python
   ...
   # 打印当前加速度，并设置为70%。
   print _acc_rate
   _acc_rate=70
   ...
   end
```