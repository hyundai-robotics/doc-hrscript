# `send`

### 描述

通过调用 `Sci` 的 `send` 发送字符串。

### 语法

&lt;Sci object&gt;.send "string" <br>
&lt;Sci object&gt;.send string variable


### 示例

```python
sci2.send "test"
or
var msg="test"
sci2.send msg
```